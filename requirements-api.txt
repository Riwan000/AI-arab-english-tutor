# FastAPI backend only — no Streamlit
# Use when API and frontend deploy separately: pip install -r requirements-api.txt
# For full local dev (API + Streamlit): pip install -r requirements.txt

httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.6.0
pyyaml>=6.0.0

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic-settings>=2.2.0
